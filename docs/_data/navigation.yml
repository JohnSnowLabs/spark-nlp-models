header:
  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /

  - titles:
      # @start locale config
      en      : &EN       Explore Models
      # @end locale config
    url: /latest.html

  - titles:
      # @start locale config
      en      : &EN       Add Your Model
      # @end locale config
    url: /latest.html

  - titles:
      # @start locale config
      en      : &EN       Demos
      # @end locale config
    url: /latest.html

  - titles:
      # @start locale config
      en      : &EN       FAQ
       # @end locale config
    url: /FAQ.html
